<nav class="navbar bg-dark navbar-expand-lg shadow fixed-top navbar-dark">
    <div class="container">
        <a class="navbar-brand" href="{{ path('homepage') }}">
            <img src="{{ asset('img/logo.svg') }}" alt="Logo Todo" width="30" height="24" class="d-inline-block align-text-top" />
            To Do List app
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">                        
                <li class="nav-item">
                    <a class="nav-link {{ 'task_list' == app.request.attributes.get('_route') ? 'active' : '' }}" href="{{ path('task_list') }}">
                        Tâches
                    </a>
                </li>
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item">
                        <a class="nav-link {{ 'task_create' == app.request.attributes.get('_route') ? 'active' : '' }}" href="{{ path('task_create') }}">
                            Nouvelle tâche
                        </a>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link {{ 'user_list' == app.request.attributes.get('_route') ? 'active' : '' }}" href="{{ path('user_list') }}">
                            Membres
                        </a>
                    </li>
                {% endif %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Compte
                    </a>
                    <ul class="dropdown-menu">
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                            <li><a class="dropdown-item" href="{{ path('app_profile') }}">Profil</a></li>
                            <li><a class="dropdown-item" href="{{ path('logout') }}">Se déconnecter</a></li>
                        {% else %}
                            <li><a class="dropdown-item" href="{{ path('app_register') }}">S'inscrire</a></li>
                            <li><a class="dropdown-item" href="{{ path('login') }}">Se connecter</a></li>
                        {% endif %}
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>