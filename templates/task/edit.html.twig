{% extends 'base.html.twig' %}

{% block title %}{{ task.title }} | {{ parent() }}{% endblock %}

{% block body %}
    <header>
        <h2 class="h1 mb-4">Editer une tâche</h2>
    </header>
    <div class="row">
        {{ form_start(form, {'action' : path('task_edit', { 'id' : task.id })}) }}
            {{ form_widget(form) }}

            <button type="submit" class="btn btn-success pull-right">Modifier</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
